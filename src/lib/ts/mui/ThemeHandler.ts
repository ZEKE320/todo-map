import createTheme from "@mui/material/styles/createTheme";

export const darkTheme = createTheme({
  palette: {
    mode: "dark",
    background: {
      paper: "#1d2429",
      default: "#1d2429",
    },
  },
});
