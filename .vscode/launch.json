{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Webpack: Chrome",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:8080",
      "webRoot": "${workspaceFolder}",
      "userDataDir": "${workspaceFolder}/.vscode/chrome/webpack"
    }
  ]
}
